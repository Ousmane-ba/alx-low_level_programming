#include "main.h"

/**
* _strchr - Function that locates a character in  a string
* @s: input in the string
* @c: occurence of the character
* Return: Always 0 (Success)
*/

char *_strchr(char *s, char c);
